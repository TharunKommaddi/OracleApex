--
-- Materialized_View "MV_CUSTOMERS"
--
CREATE MATERIALIZED VIEW "WKSP_THARUN"."MV_CUSTOMERS" ("FIRST_NAME", "LAST_NAME", "STREET", "CITY", "STATE", "DUMMY")
  DEFAULT COLLATION "USING_NLS_COMP"  SEGMENT CREATION IMMEDIATE
  ORGANIZATION HEAP PCTFREE 10 PCTUSED 40 INITRANS 10 MAXTRANS 255 
 NOCOMPRESS LOGGING
  STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
  PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1
  BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
  TABLESPACE "DATA" 
  BUILD IMMEDIATE
  USING INDEX 
  REFRESH FORCE ON DEMAND
  USING DEFAULT LOCAL ROLLBACK SEGMENT
  USING ENFORCED CONSTRAINTS DISABLE ON QUERY COMPUTATION DISABLE QUERY REWRITE DISABLE CONCURRENT REFRESH
  AS SELECT
  c.first_name,
  c.last_name,
  a.street,
  a.city,
  a.state,
  CAST(NULL AS VARCHAR2(1)) AS dummy
FROM
  T_ORACLE_TEXT_CUSTOMERS c
JOIN
  T_ORACLE_TEXT_ADDRESSES a ON c.customer_id = a.customer_id
/